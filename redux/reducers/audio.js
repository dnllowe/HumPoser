'use strict';

const initialState = {
}

// ACTIONS

// REDUCER
const reducer = (state = initialState, action) => {
  let newState = Object.assign({}, state);
  return newState;
}

// ACTION CREATORS

export default reducer;